import { changeRole, getOrganization } from "@/app/team/actions";

export default async function AdminTeamPage() {
  const organization = await getOrganization();
  const managers = organization.managers;
  const renderMember = (member: { id: string; name: string; email: string; managerId?: string | null }, role: "employee" | "manager" | "user") => <article className="admin-inquiry" key={member.id}><div><strong>{member.name}</strong><p>{member.email}</p></div><form action={async (formData) => { "use server"; const selectedRole = String(formData.get("role")); const selectedManager = String(formData.get("managerId") || ""); await changeRole(member.id, selectedRole === "manager" ? "manager" : selectedRole === "employee" ? "employee" : "user", selectedManager || null); }}><select name="role" defaultValue={role} aria-label={`Role for ${member.name}`}><option value="user">Unassigned</option><option value="employee">Employee</option><option value="manager">Manager</option></select><select name="managerId" defaultValue={member.managerId ?? ""} aria-label={`Manager for ${member.name}`}><option value="">No manager</option>{managers.map((manager) => <option key={manager.id} value={manager.id}>{manager.name}</option>)}</select><button className="button" type="submit">Save</button></form></article>;
  return <div className="admin-page"><section className="admin-section"><div className="admin-heading"><div><p className="eyebrow">Owner / Organization</p><h1>Team structure.</h1><p className="hero-lead">Assign roles and keep every team visible from one place.</p></div></div><div className="admin-list"><h2>Owners</h2>{organization.owners.map((member) => <article className="admin-inquiry" key={member.id}><div><strong>{member.name}</strong><p>{member.email} · Owner access</p></div></article>)}<h2>Managers</h2>{organization.managers.map((member) => renderMember(member, "manager"))}<h2>Employees</h2>{organization.employees.map((member) => renderMember(member, "employee"))}<h2>Unassigned users</h2>{organization.unassigned.map((member) => renderMember(member, "user"))}</div></section></div>;
}

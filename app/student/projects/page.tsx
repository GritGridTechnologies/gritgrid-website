import { SiteShell, PageHero } from "@/components/site-shell";
import { projects } from "@/lib/projects";

export default function StudentProjectsPage() { return <SiteShell><PageHero eyebrow="Student catalogue" title="Projects with a clear path forward." intro="Explore the existing GritGrid project catalogue by category, then request a scoped build for your academic or technical goals." /><main className="section student-catalogue"><div className="container"><div className="catalogue-tools"><input aria-label="Search projects" placeholder="Search projects" /><select aria-label="Filter by category" defaultValue="all"><option value="all">All categories</option><option>AI & Machine Learning</option><option>Web Development</option><option>Data & Databases</option><option>Cybersecurity</option></select><select aria-label="Filter by difficulty" defaultValue="all"><option value="all">All difficulty levels</option><option>Foundation</option><option>Intermediate</option><option>Advanced</option></select></div><div className="student-catalogue-grid">{projects.map((project) => <article className="student-project-card" key={project.slug}><p className="eyebrow">{project.category}</p><h2>{project.title}</h2><p>{project.description}</p><div className="tag-list">{project.technologies.slice(0, 4).map((tech) => <span key={tech}>{tech}</span>)}</div><div className="card-actions"><a className="text-link" href={`/student/projects/${project.slug}`}>View details →</a><a className="button button-primary" href="/student/request">Request project</a></div></article>)}</div></div></main></SiteShell>; }

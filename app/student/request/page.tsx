"use client";

import { useState } from "react";
import { SiteShell, PageHero } from "@/components/site-shell";

export default function StudentRequestPage() { const [message, setMessage] = useState(""); const [loading, setLoading] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setMessage(""); const response = await fetch("/api/student-requests", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(new FormData(event.currentTarget))) }); const data = await response.json(); setMessage(response.ok ? `Request submitted successfully. Reference ID: ${data.referenceId}` : data.error); setLoading(false); if (response.ok) event.currentTarget.reset(); } return <SiteShell><PageHero eyebrow="Request a project" title="Tell us what you want to build." intro="Share the essentials. We will review the brief and follow up with a clear scope, timeline and pricing range." /><main className="section"><div className="container"><form className="student-request-form" onSubmit={submit}><div className="request-fields">{[["fullName","Full name"],["email","Email"],["phone","Phone"],["college","College / University"],["course","Course"],["year","Year"],["projectType","Project type"],["projectTitle","Project title"],["technologyPreference","Technology preference"],["deadline","Deadline"],["budgetRange","Budget range"]].map(([name,label]) => <label key={name}>{label}<input name={name} required={['fullName','email','projectType','projectTitle'].includes(name)} /></label>)}</div><label>Requirements<textarea name="requirements" required rows={7} /></label><label>Additional requirements<textarea name="additionalRequirements" rows={4} /></label><button className="button button-primary" disabled={loading}>{loading ? "Submitting..." : "Submit project request →"}</button>{message && <p className="form-success" role="status">{message}</p>}</form></div></main></SiteShell>; }
